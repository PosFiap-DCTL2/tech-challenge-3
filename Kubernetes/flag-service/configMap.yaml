apiVersion: v1
kind: ConfigMap
metadata:
  name: flag-configmap
  namespace: flag-service
  labels:
    app: flag-service
data:
  AUTH_SERVICE_URL: ${{ secrets.AUTH_URL }}
  DATABASE_URL: ${{ secrets.FLAG_DB_URL }}
